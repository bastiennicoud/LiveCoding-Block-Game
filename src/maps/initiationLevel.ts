import * as w from '../elements/Wall'
import * as b from '../elements/Blank'

export default {
  // Define element position in the map
  pattern: [
    [w,w,w,w],
    [w,b,w,w],
    [w,b,b,b],
    [w,w,w,w]
  ]
}
